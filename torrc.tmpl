# Generated by confd {{datetime}}

# Paths
DataDirectory /data

# Ports
ORPort {{ or (getenv "ORPORT") "9001"}}
DirPort {{ or (getenv "DIRPORT") "9030"}}  # what port to advertise for directory connections

# ExitPolicy
ExitPolicy {{ or (getenv "EXITPOLICY") "reject *:* # no exits allowed"}}

# Control port
ControlPort {{ or (getenv "CONTROLPORT") "9051"}}
HashedControlPassword {{getenv "HASHEDCONTROLPASSWORD"}}

# Server infor
Nickname {{ or (getenv "NICKNAME") "ididnteditheconfig"}}
ContactInfo { or ({getenv "CONTACTINFO") "Random Person <nobody AT example dot com>"}}
MyFamily {{getenv "MYFAMILY"}}
