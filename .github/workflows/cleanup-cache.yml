name: Cleanup build cache

on:
  workflow_dispatch:
  schedule:
    - cron: '0 1 * * *' # Daily at 0 AM UTC
    
jobs:
  cleanup:
    runs-on: ubuntu-latest
    steps:
      - name: Cleanup
        run: |
          echo "Deleting caches..."
          gh cache delete --all
          echo "Done"
